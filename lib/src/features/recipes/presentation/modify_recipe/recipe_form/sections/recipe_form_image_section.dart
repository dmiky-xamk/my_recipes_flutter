import 'package:flutter/widgets.dart';

class RecipeFormImageSection extends StatelessWidget {
  const RecipeFormImageSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 200,
      child: Placeholder(),
    );
  }
}
